on:
  push:
    branches: [ "main" ]
    paths-ignore:
      - '**.md'
  pull_request:
    paths-ignore:
      - '**.md'

jobs:
  tests-pinned:
    uses: ./.github/workflows/test-reusable.yml
    with:
      requirements_file: requirements-ci.txt
      use_min_dep_versions: false
